apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amjtechblog.fullname" . }}-test-connection"
  labels:
    {{- include "amjtechblog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amjtechblog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
