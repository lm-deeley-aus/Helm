apiVersion: v1
kind: Service
metadata:
  name: {{ include "deeleyllc-prom-proj.fullname" . }}
  labels:
    {{- include "deeleyllc-prom-proj.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deeleyllc-prom-proj.selectorLabels" . | nindent 4 }}
